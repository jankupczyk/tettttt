for T in 140173111 140172961 140172423 140168535; do R=$(onstat -g act | awk -v t="$T" '$1==t{print $3}'); [ -z "$R" ] && { echo "tid $T no running, skipped..."; continue; }; S=$(onstat -u | grep -F "$R" | awk '{print $3}'); [ -z "$S" ] && continue; echo "=== tid $T -> sid $S ==="; onstat -g sql $S | sed -n '/^Sess/,$p'; done
