for ym in $(ls -l --time-style=+%Y-%m | awk '{print $6}' | grep -v '^2025-' | sort -u); do
    files=$(ls -l --time-style=+%Y-%m | awk -v m="$ym" '$6==m {print $7}')
    zip "/app/${ym}.zip" $files && rm -f $files
done
